@model IEnumerable<RentalManagementSystem.Models.RentalFloors.Floors>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<RadzenGrid Data="@Model" TItem="RentalManagementSystem.Models.RentalFloors.Floors">
    <Columns>
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Property="FloorId" Title="Floor ID" />
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Property="PropertyName" Title="Property Name" />
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Property="FloorNo" Title="Floor No" />
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Property="RoomType" Title="Room Type" />
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Property="Status" Title="Status" />
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Property="CreateDate" Title="Create Date" />
        <RadzenGridColumn TItem="RentalManagementSystem.Models.RentalFloors.Floors" Title="Actions">
            <Template Context="item">
                <RadzenButton Text="Edit" Click="@(args => EditItem(item))" />
                <RadzenButton Text="Details" Click="@(args => ViewDetails(item))" />
                <RadzenButton Text="Delete" Click="@(args => DeleteItem(item))" />
            </Template>
        </RadzenGridColumn>
    </Columns>
</RadzenGrid>

@code {
    void EditItem(RentalManagementSystem.Models.RentalFloors.Floors item)
    {
        // Implement edit logic here
    }

    void ViewDetails(RentalManagementSystem.Models.RentalFloors.Floors item)
    {
        // Implement view details logic here
    }

    void DeleteItem(RentalManagementSystem.Models.RentalFloors.Floors item)
    {
        // Implement delete logic here
    }
}
